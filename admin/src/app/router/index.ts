import { createRouter, createWebHistory } from 'vue-router'

// 占位实现：Task 13 将重写为真实路由（含动态路由注册与登录守卫）
const router = createRouter({
  history: createWebHistory(),
  routes: [],
})

export default router
