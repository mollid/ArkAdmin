<?php

namespace Addons\{{ADDON}}\Models;

use Illuminate\Database\Eloquent\Model;

/**
 * ark:crud 生成（表 {{TABLE}}）：可再手工修改，重新生成需 --force 覆盖。
 */
class {{MODEL}} extends Model
{
    protected $table = '{{TABLE}}';

    protected $fillable = [
{{FILLABLE}}
    ];

    protected $casts = [
{{CASTS}}
    ];
}
