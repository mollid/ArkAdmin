# 本地开发无需该文件：vite.config.ts 已把同源 /api 代理到 http://localhost:8080
#
# 生产构建（EdgeOne Makers / GitHub Actions）前后端不同源时必填：
# 必须是后端根地址 + /api/admin，且后端需允许该来源跨域（Laravel 默认 cors 配置已放行 api/*）
# 例：VITE_API_BASE=https://api.example.com/api/admin
VITE_API_BASE=
